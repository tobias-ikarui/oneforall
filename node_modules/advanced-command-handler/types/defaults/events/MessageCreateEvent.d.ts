import { Message } from 'discord.js';
import { CommandHandler } from '../..';
import { Event, EventContext } from '../../classes';
import MessageCreateOptions = CommandHandler.MessageCreateOptions;
export declare class MessageCreateEvent extends Event {
    static options: MessageCreateOptions;
    readonly name = "messageCreate";
    run(ctx: EventContext<this>, message: Message): Promise<Message | undefined>;
}
