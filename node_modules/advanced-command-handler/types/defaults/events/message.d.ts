import { Message } from 'discord.js';
import { Event, EventContext } from '../../classes';
export declare class MessageEvent extends Event {
    name: "message";
    run(ctx: EventContext<this>, message: Message): Promise<any>;
}
