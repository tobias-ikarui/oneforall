"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.PingCommand = void 0;
const classes_1 = require("../../classes");
class PingCommand extends classes_1.Command {
    constructor() {
        super(...arguments);
        this.category = 'utils';
        this.description = 'Get the ping of the bot.';
        this.name = 'ping';
        this.tags = ['guildOnly'];
        this.userPermissions = ['MANAGE_MESSAGES'];
    }
    async run(ctx) {
        const msg = await ctx.reply('Ping ?');
        const botPing = ctx.client.ping;
        const apiPing = msg.createdTimestamp - ctx.message.createdTimestamp;
        await msg.edit(`WS Ping: **${botPing}**ms\nAPI Latency: **${apiPing}**ms`);
    }
}
exports.PingCommand = PingCommand;
