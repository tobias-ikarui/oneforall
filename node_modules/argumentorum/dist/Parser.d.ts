import { Cursor } from './Cursor';
import { NamedArgumentToken, PositionalArgumentToken } from './Tokens';
export declare class Parser {
    input: string;
    cursor: Cursor;
    constructor(input: string);
    get hasNext(): boolean;
    parseNamed(): NamedArgumentToken[];
    peekNext(): PositionalArgumentToken | undefined;
    parseNext(): PositionalArgumentToken | undefined;
    consumeRemaining(): string;
    consumeWhile(predicate: (char: string) => boolean): string | undefined;
    peekRemaining(): string;
    peekWhile(predicate: (char: string) => boolean): string | undefined;
}
